Django>=5.0,<5.3

pypdf>=5.0,<7.0
python-docx>=1.1,<2.0

# Production deployment / PostgreSQL for concurrent multi-user workloads
psycopg[binary]>=3.2,<4
gunicorn>=23,<24

# Shared Redis cache for multi-worker production deployments
redis>=5.0,<7.0
