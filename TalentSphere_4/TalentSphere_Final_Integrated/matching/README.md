# Integrated Job Matching

This app integrates the Milestone 2 matching logic into TalentSphere.

It compares candidate skills with each job's required skills and returns:
- matching skills
- missing skills
- match percentage
- match score

It reuses the existing TalentSphere database models and does not create
duplicate CandidateProfile or Job tables.
